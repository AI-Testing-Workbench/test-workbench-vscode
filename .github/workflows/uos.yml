name: Build for Debian 10

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to build'
        required: true
        default: 'feature/uos-workflow'
      version:
        description: 'Package version'
        required: false
        default: '1.116.0'
      custom_extension_repo:
        description: 'Custom extension repository (optional)'
        required: false
        default: 'https://github.com/AI-Testing-Workbench/test-workbench-vsix.git'
      custom_extension_branch:
        description: 'Custom extension branch'
        required: false
        default: 'feature/uos-workflow'

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.inputs.branch }}
          fetch-depth: 1

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20.x'

      - name: 🔧 Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential pkg-config libgtk-3-dev libxkbfile-dev \
            libkrb5-dev libgbm-dev rpm libx11-dev libxss-dev libxtst-dev \
            libnss3-dev libasound2-dev libglib2.0-dev libcairo2-dev \
            libpango1.0-dev libatk1.0-dev libgdk-pixbuf2.0-dev \
            python3 git wget curl tree xz-utils \
            dpkg-dev fakeroot lintian rsync jq

      - name: 🔧 Fix TypeScript scripts
        run: |
          echo 'console.log("Preinstall OK");' > build/npm/preinstall.js
          sed -i 's/"preinstall": "node build\/npm\/preinstall.ts"/"preinstall": "node build\/npm\/preinstall.js"/' package.json
          echo 'console.log("Postinstall OK");' > build/npm/postinstall.js
          sed -i 's/"postinstall": "node build\/npm\/postinstall.ts"/"postinstall": "node build\/npm\/postinstall.js"/' package.json

      - name: 📦 Install dependencies
        run: |
          echo "=========================================="
          echo "📦 Installing dependencies"
          echo "=========================================="
          npm install --legacy-peer-deps

      - name: 📦 Install build tools
        run: |
          echo "=========================================="
          echo "📦 Installing build tools"
          echo "=========================================="

          npm install -g tsx

          npm install --legacy-peer-deps \
            ternary-stream \
            vscode-gulp-watch \
            esbuild \
            jsonc-parser \
            gulp-merge-json \
            gulp-tsb \
            gulp-sourcemaps \
            gulp-filter \
            gulp-rename \
            gulp-sort \
            gulp \
            vinyl \
            vinyl-fs \
            del \
            fs-extra \
            glob \
            minimatch \
            semver \
            @vscode/ripgrep \
            through2 \
            merge-stream \
            event-stream \
            2>/dev/null || true

      # ============================================================
      # 修复所有扩展相关文件 - 一次性完成
      # ============================================================
      - name: 🔧 Fix all extension build files
        run: |
          echo "=========================================="
          echo "🔧 Fixing all extension build files"
          echo "=========================================="

          # 1. 创建 builtInExtensions.ts
          cat > build/lib/builtInExtensions.ts << 'BUILTIN_EOF'
          /*---------------------------------------------------------------------------------------------
           *  Copyright (c) Microsoft Corporation. All rights reserved.
           *  Licensed under the MIT License. See License.txt in the project root for license information.
           *--------------------------------------------------------------------------------------------*/

          import { Stream, Readable } from 'stream';

          export const builtInExtensions: any[] = [];
          export const shouldDownloadBuiltInExtensions = false;
          export const webBuiltInExtensions: any[] = [];
          export const shouldDownloadWebBuiltInExtensions = false;

          function createEmptyStream(): Stream {
              const s = new Readable();
              s._read = () => s.push(null);
              return s;
          }

          export function getExtensionStream(): Stream { return createEmptyStream(); }
          export function getExtensionDownloadStream(): Stream { return createEmptyStream(); }
          export function packageMarketplaceExtensionsStream(): Stream { return createEmptyStream(); }
          export function packageNativeLocalExtensionsStream(): Stream { return createEmptyStream(); }
          export function packageNonNativeLocalExtensionsStream(): Stream { return createEmptyStream(); }
          export function packageLocalExtensionsStream(): Stream { return createEmptyStream(); }
          export function packageExtensionsStream(): Stream { return createEmptyStream(); }
          export function packageCopilotExtensionStream(): Stream { return createEmptyStream(); }
          export function packageCopilotChatExtensionStream(): Stream { return createEmptyStream(); }
          export function packageCopilotAgentsExtensionStream(): Stream { return createEmptyStream(); }
          export function getNativeLocalExtensionStream(): Stream { return createEmptyStream(); }
          export function getLocalExtensionStream(): Stream { return createEmptyStream(); }
          export function getMarketplaceExtensionStream(): Stream { return createEmptyStream(); }

          export function fromMarketplace(): any[] { return []; }

          export async function fetchUrls(urls: string[], options: any): Promise<void> {
              console.log(`fetchUrls: Skipping ${urls.length} URLs (UOS build)`);
              return Promise.resolve();
          }

          export function buildExtensionMedia(): void {}
          export function compileExtensionMedia(): void {}
          export function buildExtensionMediaTask(done: any): void { done(); }

          export const isWebExtension = false;
          export const builtInExtensionsProd = [];
          export const shouldDownloadBuiltInExtensionsProd = false;
          BUILTIN_EOF

          echo "✅ builtInExtensions.ts created"

          # 2. 创建 extensions.ts - 完全重写，无语法错误
          cat > build/lib/extensions.ts << 'EXTENSIONS_EOF'
          /*---------------------------------------------------------------------------------------------
           *  Copyright (c) Microsoft Corporation. All rights reserved.
           *  Licensed under the MIT License. See License.txt in the project root for license information.
           *--------------------------------------------------------------------------------------------*/

          import { Stream, Readable } from 'stream';

          function createEmptyStream(): Stream {
              const s = new Readable();
              s._read = () => s.push(null);
              return s;
          }

          export const builtInExtensions: any[] = [];
          export const shouldDownloadBuiltInExtensions = false;
          export const webBuiltInExtensions: any[] = [];
          export const shouldDownloadWebBuiltInExtensions = false;

          export function getExtensionStream(): Stream { return createEmptyStream(); }
          export function getExtensionDownloadStream(): Stream { return createEmptyStream(); }
          export function packageMarketplaceExtensionsStream(): Stream { return createEmptyStream(); }
          export function packageNativeLocalExtensionsStream(): Stream { return createEmptyStream(); }
          export function packageNonNativeLocalExtensionsStream(): Stream { return createEmptyStream(); }
          export function packageLocalExtensionsStream(): Stream { return createEmptyStream(); }
          export function packageExtensionsStream(): Stream { return createEmptyStream(); }
          export function packageCopilotExtensionStream(): Stream { return createEmptyStream(); }
          export function packageCopilotChatExtensionStream(): Stream { return createEmptyStream(); }
          export function packageCopilotAgentsExtensionStream(): Stream { return createEmptyStream(); }
          export function getNativeLocalExtensionStream(): Stream { return createEmptyStream(); }
          export function getLocalExtensionStream(): Stream { return createEmptyStream(); }
          export function getMarketplaceExtensionStream(): Stream { return createEmptyStream(); }

          export function fromMarketplace(): any[] { return []; }

          export async function fetchUrls(urls: string[], options: any): Promise<void> {
              console.log(`fetchUrls: Skipping ${urls.length} URLs (UOS build)`);
              return Promise.resolve();
          }

          export function buildExtensionMedia(): void {}
          export function compileExtensionMedia(): void {}
          export function buildExtensionMediaTask(done: any): void { done(); }

          export const isWebExtension = false;
          export const builtInExtensionsProd = [];
          export const shouldDownloadBuiltInExtensionsProd = false;

          export function scanBuiltinExtensions(): any[] { return []; }
          export function getExtensionGalleryResources(): any { return { extensions: [] }; }
          export function packageExtension(): Stream { return createEmptyStream(); }
          export function packageLocalExtension(): Stream { return createEmptyStream(); }

          export const localExtensions: any[] = [];
          export const marketplaceExtensions: any[] = [];
          export const nonNativeLocalExtensions: any[] = [];

          console.log('✅ extensions.ts loaded (UOS build)');
          EXTENSIONS_EOF

          echo "✅ extensions.ts created"

          # 3. 修改 gulpfile.extensions.ts
          if [ -f "build/gulpfile.extensions.ts" ]; then
              python3 << 'PYTHON_SCRIPT'
          import re

          with open('build/gulpfile.extensions.ts', 'r') as f:
              content = f.read()

          content = re.sub(
              r'function\s+packageVsix\s*\([^)]*\)\s*\{[^}]*\}',
              'function packageVsix() { console.log("packageVsix skipped for UOS build"); return Promise.resolve(); }',
              content,
              flags=re.DOTALL
          )

          content = re.sub(
              r'import\s*\{?\s*vsce\s*\}?\s*from\s*["\']@vscode/vsce["\']',
              '// vsce import disabled for UOS build',
              content
          )

          with open('build/gulpfile.extensions.ts', 'w') as f:
              f.write(content)

          print("✅ gulpfile.extensions.ts patched")
          PYTHON_SCRIPT
          fi

      - name: 🔧 Fix gulpfile spawn calls
        run: |
          python3 << 'PY_END'
          import re
          import os

          for file in ['build/gulpfile.vscode.ts', 'build/gulpfile.extensions.ts', 'build/gulpfile.ts']:
              if os.path.exists(file):
                  with open(file, 'r') as f:
                      content = f.read()
                  content = re.sub(r"spawn\('node'", "spawn('tsx'", content)
                  content = re.sub(r'spawn\("node"', 'spawn("tsx"', content)
                  content = re.sub(r"spawn\(process\.execPath", "spawn('tsx'", content)
                  with open(file, 'w') as f:
                      f.write(content)
                  print(f"✅ spawn calls fixed in {os.path.basename(file)}")
          PY_END

      - name: 🔧 Replace copilot.ts
        run: |
          cat > build/lib/copilot.ts << 'EOF'
          /*---------------------------------------------------------------------------------------------
           *  Copyright (c) Microsoft Corporation. All rights reserved.
           *  Licensed under the MIT License. See License.txt in the project root for license information.
           *--------------------------------------------------------------------------------------------*/

          export async function prepareBuiltInCopilotExtensionShims(extRoot: string): Promise<void> {
              console.log('Copilot extension shims preparation skipped for UOS build');
              return;
          }

          export function copyCopilotNativeDeps(productRoot: string): void {
              console.log('Copilot native deps copy skipped for UOS build');
          }

          export function getCopilotExcludeFilter(): any {
              return function() { return false; };
          }
          EOF

      - name: 🔧 Disable Crashpad
        run: |
          echo "Disabling Crashpad"
          python3 << 'PY_JSON_END'
          import json
          import os
          if os.path.exists('product.json'):
              with open('product.json', 'r') as f:
                  data = json.load(f)
              data['enableCrashReport'] = False
              if 'crashReporter' not in data:
                  data['crashReporter'] = {}
              data['crashReporter']['disabled'] = True
              with open('product.json', 'w') as f:
                  json.dump(data, f, indent=2)
              print("✅ product.json modified")
          PY_JSON_END

          mkdir -p src/vs/platform/crash/common
          cat > src/vs/platform/crash/common/crashService.ts << 'CRASH_EOF'
          /*---------------------------------------------------------------------------------------------
           *  Copyright (c) Microsoft Corporation. All rights reserved.
           *  Licensed under the MIT License. See License.txt in the project root for license information.
           *--------------------------------------------------------------------------------------------*/
          import { createDecorator } from '../../../base/common/instantiation.js';
          export const ICrashService = createDecorator<ICrashService>('crashService');
          export interface ICrashService { _serviceBrand: undefined; }
          export class CrashService implements ICrashService {
              declare readonly _serviceBrand: undefined;
              constructor() { console.log('Crash service disabled for UOS build'); }
          }
          CRASH_EOF

      - name: ⚙️ Set env
        run: |
          echo "VSCODE_SKIP_BUILTIN_EXTENSION_DOWNLOAD=1" >> $GITHUB_ENV
          echo "VSCODE_ELECTRON_SKIP_DOWNLOAD=1" >> $GITHUB_ENV
          echo "VSCODE_SKIP_PRELAUNCH=1" >> $GITHUB_ENV
          echo "VSCODE_SKIP_BUNDLED_EXTENSIONS=1" >> $GITHUB_ENV
          echo "ELECTRON_DISABLE_CRASH_REPORTER=1" >> $GITHUB_ENV
          echo "ELECTRON_DISABLE_CRASH_REPORTING=1" >> $GITHUB_ENV
          echo "VSCODE_SKIP_VSIX_PACKAGING=1" >> $GITHUB_ENV

      - name: 🔨 Build VSCode
        run: |
          export NODE_OPTIONS="--max-old-space-size=8192"
          export VSCODE_SKIP_BUILTIN_EXTENSION_DOWNLOAD=1
          export VSCODE_ELECTRON_SKIP_DOWNLOAD=1
          export VSCODE_SKIP_PRELAUNCH=1
          export VSCODE_SKIP_BUNDLED_EXTENSIONS=1
          export ELECTRON_DISABLE_CRASH_REPORTER=1
          export ELECTRON_DISABLE_CRASH_REPORTING=1
          export VSCODE_SKIP_VSIX_PACKAGING=1
          echo "Start: $(date)"
          npx tsx node_modules/gulp/bin/gulp.js vscode-linux-x64 --no-color
          echo "Done: $(date)"

      - name: 🔍 Verify build contents
        run: |
          echo "=========================================="
          echo "🔍 Verify build contents"
          echo "=========================================="

          BUILD_DIR=""
          for dir in "./VSCode-linux-x64" "./vscode/VSCode-linux-x64" "../VSCode-linux-x64"; do
            if [ -d "$dir" ]; then
              BUILD_DIR="$dir"
              echo "Found build directory: $BUILD_DIR"
              break
            fi
          done

          if [ -z "$BUILD_DIR" ]; then
            echo "Build directory not found!"
            exit 1
          fi

          TARGET_DIR="./tscode"
          if [ "$BUILD_DIR" != "$TARGET_DIR" ]; then
            echo "Renaming $BUILD_DIR to $TARGET_DIR"
            rm -rf "$TARGET_DIR"
            mv "$BUILD_DIR" "$TARGET_DIR"
          fi
          BUILD_DIR="$TARGET_DIR"
          echo "BUILD_DIR=$BUILD_DIR" >> $GITHUB_ENV

          echo "Initial build size:"
          du -sh "$BUILD_DIR"

      - name: 📦 Copy node_modules to resources/app
        run: |
          echo "=========================================="
          echo "📦 Copy node_modules to resources/app"
          echo "=========================================="

          BUILD_DIR="${BUILD_DIR:-./tscode}"

          if [ ! -d "./node_modules" ]; then
            echo "❌ node_modules not found in root!"
            echo "Installing node_modules..."
            npm install --legacy-peer-deps
          fi

          echo "📌 Copying node_modules to $BUILD_DIR/resources/app/"
          rm -rf "$BUILD_DIR/resources/app/node_modules"
          cp -r ./node_modules "$BUILD_DIR/resources/app/"

          echo "✅ node_modules copied"

      # ============================================================
      # 核心修复：从官方 VSCode 复制扩展和原生模块
      # ============================================================
      - name: 🔥 Copy official extensions and native modules
        run: |
          set +e

          echo "=========================================="
          echo "🔥 COPYING OFFICIAL EXTENSIONS & NATIVE MODULES"
          echo "=========================================="

          BUILD_DIR="${BUILD_DIR:-./tscode}"
          VERSION="${{ github.event.inputs.version || '1.116.0' }}"

          echo ""
          echo "📌 Downloading official VSCode $VERSION..."

          rm -rf /tmp/official-vscode
          rm -f /tmp/official.tar.gz

          wget -q --show-progress "https://update.code.visualstudio.com/$VERSION/linux-x64/stable" -O /tmp/official.tar.gz

          if [ $? -ne 0 ] || [ ! -s /tmp/official.tar.gz ]; then
            echo "⚠️ Failed to download $VERSION, trying latest..."
            wget -q --show-progress https://update.code.visualstudio.com/latest/linux-x64/stable -O /tmp/official.tar.gz
            if [ $? -ne 0 ] || [ ! -s /tmp/official.tar.gz ]; then
              echo "❌ Failed to download official VSCode!"
              exit 1
            fi
          fi

          echo "📌 Extracting official VSCode..."
          mkdir -p /tmp/official-vscode
          tar -xzf /tmp/official.tar.gz -C /tmp/official-vscode/

          OFFICIAL_DIR=""
          for dir in "/tmp/official-vscode/VSCode-linux-x64" "/tmp/official-vscode/vscode/VSCode-linux-x64"; do
            if [ -d "$dir" ]; then
              OFFICIAL_DIR="$dir"
              break
            fi
          done

          if [ -z "$OFFICIAL_DIR" ]; then
            OFFICIAL_DIR=$(find /tmp/official-vscode -type d -name "VSCode-linux-x64" 2>/dev/null | head -1)
          fi

          if [ -z "$OFFICIAL_DIR" ] || [ ! -d "$OFFICIAL_DIR" ]; then
            echo "❌ Could not find official VSCode directory!"
            ls -la /tmp/official-vscode/
            exit 1
          fi

          echo "✅ Official VSCode found at: $OFFICIAL_DIR"

          OFFICIAL_EXT_DIR="$OFFICIAL_DIR/resources/app/extensions"
          OFFICIAL_NM="$OFFICIAL_DIR/resources/app/node_modules"
          TARGET_EXT_DIR="$BUILD_DIR/resources/app/extensions"
          TARGET_NM="$BUILD_DIR/resources/app/node_modules"

          # ============================================================
          # 1. 复制所有扩展（使用 cp -r 替代 rsync，更可靠）
          # ============================================================
          echo ""
          echo "📌 Step 1: Copying ALL official extensions..."

          if [ ! -d "$OFFICIAL_EXT_DIR" ]; then
            echo "❌ Official extensions directory not found!"
            exit 1
          fi

          # 先清空目标扩展目录
          echo "  Clearing target extensions directory..."
          rm -rf "$TARGET_EXT_DIR"
          mkdir -p "$TARGET_EXT_DIR"

          # 使用 cp -r 复制所有扩展
          echo "  Copying extensions from: $OFFICIAL_EXT_DIR"
          cp -r "$OFFICIAL_EXT_DIR"/* "$TARGET_EXT_DIR/" 2>/dev/null

          # 验证复制结果
          if [ -d "$TARGET_EXT_DIR" ]; then
              EXT_COUNT=$(find "$TARGET_EXT_DIR" -maxdepth 1 -type d ! -path "$TARGET_EXT_DIR" 2>/dev/null | wc -l)
              echo "  ✅ Copied $EXT_COUNT extensions"

              # 检查关键扩展
              echo "  🔍 Checking critical extensions:"
              for ext in typescript-language-features git json-language-features markdown-language-features html-language-features css-language-features npm; do
                  if [ -d "$TARGET_EXT_DIR/$ext" ]; then
                      echo "    ✅ $ext"
                  else
                      echo "    ❌ $ext MISSING"
                  fi
              done
          else
              echo "  ❌ Failed to copy extensions!"
              exit 1
          fi

          # ============================================================
          # 2. 复制官方 node_modules 中的关键模块
          # ============================================================
          echo ""
          echo "📌 Step 2: Copying missing Node modules from official..."

          cd "$BUILD_DIR/resources/app"

          MISSING_MODULES=(
              "vscode-languageclient"
              "vscode-languageserver"
              "typescript"
          )

          for mod in "${MISSING_MODULES[@]}"; do
              if [ -d "$OFFICIAL_NM/$mod" ]; then
                  echo "  📦 Copying: $mod"
                  rm -rf "node_modules/$mod"
                  cp -r "$OFFICIAL_NM/$mod" "node_modules/"
                  if [ -d "node_modules/$mod" ]; then
                      echo "    ✅ $mod copied"
                  fi
              else
                  echo "  ⚠️ $mod not found in official, trying npm install..."
                  npm install "$mod" --legacy-peer-deps 2>/dev/null
                  if [ -d "node_modules/$mod" ]; then
                      echo "    ✅ $mod installed via npm"
                  fi
              fi
          done

          # ============================================================
          # 3. 替换原生模块为兼容版本
          # ============================================================
          echo ""
          echo "📌 Step 3: Replacing native modules with compatible versions..."

          NODE_FILES=(
              "@vscode/sqlite3/build/Release/vscode-sqlite3.node"
              "@vscode/spdlog/build/Release/spdlog.node"
          )

          for file in "${NODE_FILES[@]}"; do
              SRC="$OFFICIAL_NM/$file"
              DST="$TARGET_NM/$file"

              if [ -f "$SRC" ]; then
                  echo "  📦 Replacing: $file"
                  mkdir -p "$(dirname "$DST")"
                  cp "$SRC" "$DST"
                  chmod 644 "$DST"
                  echo "    ✅ Replaced"
              else
                  echo "  ⚠️ Source not found: $file"
              fi
          done

          # 复制 ripgrep
          if [ -f "$OFFICIAL_NM/@vscode/ripgrep/bin/rg" ]; then
              echo "  📦 Copying: @vscode/ripgrep/bin/rg"
              mkdir -p "$TARGET_NM/@vscode/ripgrep/bin"
              cp "$OFFICIAL_NM/@vscode/ripgrep/bin/rg" "$TARGET_NM/@vscode/ripgrep/bin/rg"
              chmod 755 "$TARGET_NM/@vscode/ripgrep/bin/rg"
              echo "    ✅ Copied"
          fi

          # ============================================================
          # 4. 修复所有二进制权限
          # ============================================================
          echo ""
          echo "📌 Step 4: Fixing ALL binary permissions..."

          # 修复 testagent
          find "$TARGET_EXT_DIR" -name "testagent" -type f 2>/dev/null | while read -r file; do
              chmod +x "$file"
              echo "  ✅ Fixed: $(basename $file)"
          done

          # 修复 testflow
          find "$TARGET_EXT_DIR" -name "testflow" -type f 2>/dev/null | while read -r file; do
              chmod +x "$file"
              echo "  ✅ Fixed: $(basename $file)"
          done

          # 修复所有 bin 目录
          find "$TARGET_EXT_DIR" -type d -name "bin" 2>/dev/null | while read -r dir; do
              find "$dir" -type f -exec chmod +x {} \; 2>/dev/null
              echo "  ✅ Fixed permissions in: $dir"
          done

          # 修复所有 scripts 目录
          find "$TARGET_EXT_DIR" -type d -name "scripts" 2>/dev/null | while read -r dir; do
              find "$dir" -type f -exec chmod +x {} \; 2>/dev/null
              echo "  ✅ Fixed permissions in: $dir"
          done

          # ============================================================
          # 5. 最终验证
          # ============================================================
          echo ""
          echo "📌 Step 5: Final verification..."

          FINAL_EXT_COUNT=$(find "$TARGET_EXT_DIR" -maxdepth 1 -type d ! -path "$TARGET_EXT_DIR" 2>/dev/null | wc -l)
          echo "  📦 Total extensions: $FINAL_EXT_COUNT"

          # 检查关键扩展
          ALL_CRITICAL_EXISTS=true
          for ext in typescript-language-features git json-language-features; do
              if [ ! -d "$TARGET_EXT_DIR/$ext" ]; then
                  echo "  ❌ $ext still missing!"
                  ALL_CRITICAL_EXISTS=false
              fi
          done

          if [ "$ALL_CRITICAL_EXISTS" = true ]; then
              echo "  ✅ All critical extensions present!"
          else
              echo "  ⚠️ Some critical extensions missing"
          fi

          # ============================================================
          # 6. 清理
          # ============================================================
          echo ""
          echo "📌 Cleaning up..."
          rm -f /tmp/official.tar.gz
          rm -rf /tmp/official-vscode

          echo ""
          echo "✅ ALL FIXES COMPLETED!"

          set -e

      - name: 📦 Download and prepare custom extensions
        run: |
          echo "=========================================="
          echo "📦 Downloading and preparing custom extensions"
          echo "=========================================="

          CUSTOM_REPO="${{ github.event.inputs.custom_extension_repo }}"
          CUSTOM_BRANCH="${{ github.event.inputs.custom_extension_branch || 'feature/uos-workflow' }}"

          BUILD_DIR="${BUILD_DIR:-./tscode}"
          mkdir -p "$BUILD_DIR/resources/app/custom-extensions"

          if [ -n "$CUSTOM_REPO" ] && [ "$CUSTOM_REPO" != "null" ]; then
            echo "📌 Cloning custom extension from: $CUSTOM_REPO"
            rm -rf /tmp/custom-ext-temp
            git clone --depth 1 --branch "$CUSTOM_BRANCH" "$CUSTOM_REPO" /tmp/custom-ext-temp 2>/dev/null || true

            if [ -d "/tmp/custom-ext-temp" ] && [ "$(ls -A /tmp/custom-ext-temp 2>/dev/null)" ]; then
              find /tmp/custom-ext-temp -name "*.vsix" -type f 2>/dev/null | while read vsix_file; do
                vsix_name=$(basename "$vsix_file" .vsix)
                if [[ "$vsix_name" == *"darwin"* ]] || [[ "$vsix_name" == *"win32"* ]]; then
                  echo "  ⏭️ Skipping non-Linux: $vsix_name"
                  continue
                fi
                echo "  📌 Extracting: $vsix_name"
                mkdir -p /tmp/extract-vsix-$vsix_name
                unzip -q "$vsix_file" -d /tmp/extract-vsix-$vsix_name 2>/dev/null
                if [ -d "/tmp/extract-vsix-$vsix_name/extension" ]; then
                  mkdir -p "$BUILD_DIR/resources/app/custom-extensions/$vsix_name"
                  cp -r /tmp/extract-vsix-$vsix_name/extension/* "$BUILD_DIR/resources/app/custom-extensions/$vsix_name/"
                elif [ -f "/tmp/extract-vsix-$vsix_name/package.json" ]; then
                  mkdir -p "$BUILD_DIR/resources/app/custom-extensions/$vsix_name"
                  cp -r /tmp/extract-vsix-$vsix_name/* "$BUILD_DIR/resources/app/custom-extensions/$vsix_name/"
                fi
                rm -rf /tmp/extract-vsix-$vsix_name
              done
              rm -rf /tmp/custom-ext-temp
            fi
          fi

      - name: 🧹 Deep cleanup
        run: |
          echo "=========================================="
          echo "🧹 Deep cleanup"
          echo "=========================================="

          BUILD_DIR="${BUILD_DIR:-./tscode}"

          rm -rf "$BUILD_DIR/resources/app/extensions/copilot" 2>/dev/null || true
          rm -rf "$BUILD_DIR/resources/app/extensions"/testagent-nodejs-tscode-darwin-arm64 2>/dev/null || true
          rm -rf "$BUILD_DIR/resources/app/extensions"/testagent-nodejs-tscode-win32-x64 2>/dev/null || true

          find "$BUILD_DIR/resources/app/extensions" -name "*.ts" -type f ! -name "*.d.ts" -delete 2>/dev/null || true
          find "$BUILD_DIR/resources/app/extensions" -name "*.map" -type f -delete 2>/dev/null || true
          find "$BUILD_DIR/resources/app/extensions" -type d -name "src" -exec rm -rf {} + 2>/dev/null || true
          find "$BUILD_DIR/resources/app/extensions" -type d -name "test" -exec rm -rf {} + 2>/dev/null || true

      - name: 📄 Create launcher
        run: |
          echo "=========================================="
          echo "📄 Creating launcher"
          echo "=========================================="

          BUILD_DIR="${BUILD_DIR:-./tscode}"
          cd "$BUILD_DIR"

          [ -f "resources/app/package.json" ] && cp "resources/app/package.json" .
          [ -f "resources/app/product.json" ] && cp "resources/app/product.json" .

          REAL_EXEC=""
          for name in code code-oss; do
            if [ -f "./$name" ]; then
              REAL_EXEC="$name"
              break
            fi
          done

          if [ -z "$REAL_EXEC" ]; then
            REAL_EXEC=$(find . -maxdepth 1 -type f -executable ! -name "*.so" ! -name "*.sh" ! -name "chrome-*" 2>/dev/null | head -1 | sed 's|^\./||')
          fi

          if [ -z "$REAL_EXEC" ]; then
            echo "❌ No executable found!"
            exit 1
          fi

          [ "$REAL_EXEC" != "tscode" ] && mv "$REAL_EXEC" "tscode"

          cat > run.sh << 'RUNEOF'
          #!/bin/bash
          SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
          export ELECTRON_ENABLE_GPU=0
          export LIBGL_ALWAYS_SOFTWARE=1
          export NODE_OPTIONS="--max-old-space-size=4096"
          export ELECTRON_DISABLE_SANDBOX=1
          export GDK_BACKEND=x11
          export QT_QPA_PLATFORM=xcb
          export ELECTRON_DISABLE_CRASH_REPORTER=1
          export ELECTRON_DISABLE_CRASH_REPORTING=1
          export CRASHPAD_DISABLE=1
          export GNOME_KEYRING_CONTROL=
          export DBUS_SESSION_BUS_ADDRESS=
          export PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring
          export SECRETS_BACKEND=basic
          export DISABLE_VAULT=1
          export VSCODE_PASSWORD_STORE=basic
          export ELECTRON_PASSWORD_STORE=basic
          export VSCODE_APPDATA="$HOME/.local/share/tscode"
          export VSCODE_EXTENSIONS_DIR="$HOME/.tscode/extensions"
          mkdir -p "$VSCODE_APPDATA" 2>/dev/null || true
          mkdir -p "$VSCODE_EXTENSIONS_DIR" 2>/dev/null || true
          export VSCODE_SYSTEM_EXTENSIONS="$SCRIPT_DIR/resources/app/extensions"
          exec "$SCRIPT_DIR/tscode" \
              --no-sandbox \
              --disable-gpu-sandbox \
              --disable-gpu \
              --disable-crash-reporter \
              --password-store="basic" \
              --extensions-dir="$VSCODE_EXTENSIONS_DIR" \
              --user-data-dir="$VSCODE_APPDATA" \
              --builtin-extensions-dir="$VSCODE_SYSTEM_EXTENSIONS" \
              "$@"
          RUNEOF

          chmod +x run.sh
          chmod +x tscode
          chmod +x chrome-sandbox 2>/dev/null || true

      - name: 📦 Build DEB package
        run: |
          echo "=========================================="
          echo "📦 Building DEB package"
          echo "=========================================="

          BUILD_DIR="${BUILD_DIR:-./tscode}"
          VERSION="${{ github.event.inputs.version || '1.116.0' }}"
          DEB_VERSION="${VERSION}-1"
          DEB_ARCH="amd64"
          DEB_ROOT="$(pwd)/deb_root"

          mkdir -p "$DEB_ROOT/DEBIAN"
          mkdir -p "$DEB_ROOT/opt/tscode"
          mkdir -p "$DEB_ROOT/usr/bin"
          mkdir -p "$DEB_ROOT/usr/share/applications"
          mkdir -p "$DEB_ROOT/usr/share/icons/hicolor/256x256/apps"

          cp -r "$BUILD_DIR"/* "$DEB_ROOT/opt/tscode/"

          if [ -f "$DEB_ROOT/opt/tscode/chrome-sandbox" ]; then
            sudo chown root:root "$DEB_ROOT/opt/tscode/chrome-sandbox"
            sudo chmod 4755 "$DEB_ROOT/opt/tscode/chrome-sandbox"
          fi

          chmod +x "$DEB_ROOT/opt/tscode/tscode" 2>/dev/null || true
          chmod +x "$DEB_ROOT/opt/tscode/run.sh" 2>/dev/null || true

          cat > "$DEB_ROOT/usr/bin/tscode" << 'WRAPEOF'
          #!/bin/bash
          exec /opt/tscode/run.sh "$@"
          WRAPEOF
          chmod +x "$DEB_ROOT/usr/bin/tscode"

          if [ -f "$BUILD_DIR/resources/app/resources/linux/code.png" ]; then
            cp "$BUILD_DIR/resources/app/resources/linux/code.png" \
               "$DEB_ROOT/usr/share/icons/hicolor/256x256/apps/tscode.png"
          fi

          cat > "$DEB_ROOT/usr/share/applications/tscode.desktop" << 'EOF'
          [Desktop Entry]
          Version=1.0
          Type=Application
          Name=TSCode
          GenericName=Code Editor
          Comment=Code Editing. Redefined.
          Exec=/opt/tscode/run.sh %F
          Icon=tscode
          Terminal=false
          Categories=Development;IDE;
          MimeType=text/plain;inode/directory;
          StartupNotify=true
          StartupWMClass=Code
          EOF

          cat > "$DEB_ROOT/DEBIAN/control" << 'CONTROLEOF'
          Package: tscode
          Version: REPLACE_VERSION
          Architecture: REPLACE_ARCH
          Maintainer: TSCode Team <tscode@example.com>
          Description: TSCode - Code Editing. Redefined.
           TSCode is a code editor redefined and optimized for building
           and debugging modern web and cloud applications.
          Depends: libgtk-3-0, libx11-6, libxss1, libxtst6, libnss3, libasound2, libgbm1, libglib2.0-0, libcairo2, libpango-1.0-0, libatk1.0-0, libgdk-pixbuf2.0-0
          Recommends: git, python3
          Homepage: https://github.com/tscode
          Section: devel
          Priority: optional
          CONTROLEOF

          sed -i "s/REPLACE_VERSION/$DEB_VERSION/g" "$DEB_ROOT/DEBIAN/control"
          sed -i "s/REPLACE_ARCH/$DEB_ARCH/g" "$DEB_ROOT/DEBIAN/control"

          cat > "$DEB_ROOT/DEBIAN/postinst" << 'POSTINSTEOF'
          #!/bin/bash
          set -e

          echo "=========================================="
          echo "🔧 Setting up TSCode..."
          echo "=========================================="

          if [ -f "/opt/tscode/chrome-sandbox" ]; then
              chown root:root /opt/tscode/chrome-sandbox
              chmod 4755 /opt/tscode/chrome-sandbox
              echo "  ✅ chrome-sandbox SUID fixed"
          fi

          echo ""
          echo "📌 Fixing extension binaries..."

          find /opt/tscode/resources/app/extensions -name "testagent" -type f -exec chmod +x {} \; 2>/dev/null || true
          echo "  ✅ testagent permissions fixed"

          find /opt/tscode/resources/app/extensions -name "testflow" -type f -exec chmod +x {} \; 2>/dev/null || true
          echo "  ✅ testflow permissions fixed"

          find /opt/tscode/resources/app/extensions -type d -name "bin" -exec find {} -type f -exec chmod +x {} \; 2>/dev/null || true
          echo "  ✅ bin directories fixed"

          find /opt/tscode/resources/app/extensions -type d -name "scripts" -exec find {} -type f -exec chmod +x {} \; 2>/dev/null || true
          echo "  ✅ scripts directories fixed"

          echo ""
          echo "📌 Setting main executables..."
          chmod +x /opt/tscode/run.sh 2>/dev/null || true
          chmod +x /opt/tscode/tscode 2>/dev/null || true
          chmod +x /usr/bin/tscode 2>/dev/null || true

          echo ""
          echo "📌 Updating desktop database..."
          if command -v update-desktop-database >/dev/null 2>&1; then
              update-desktop-database /usr/share/applications/
              echo "  ✅ Desktop database updated"
          fi

          echo ""
          echo "=========================================="
          echo "✅ TSCode installed successfully!"
          echo ""
          echo "🚀 Run: tscode"
          echo "=========================================="

          exit 0
          POSTINSTEOF

          chmod +x "$DEB_ROOT/DEBIAN/postinst"

          cat > "$DEB_ROOT/DEBIAN/prerm" << 'PRERMEOF'
          #!/bin/bash
          set -e
          echo "TSCode is being removed..."
          echo "User data preserved at: ~/.local/share/tscode/"
          echo "Extensions preserved at: ~/.tscode/extensions/"
          exit 0
          PRERMEOF

          chmod +x "$DEB_ROOT/DEBIAN/prerm"

          fakeroot dpkg-deb -Zgzip -z9 --build "$DEB_ROOT" "tscode-${DEB_VERSION}_${DEB_ARCH}.deb"

          if [ $? -eq 0 ] && [ -f "tscode-${DEB_VERSION}_${DEB_ARCH}.deb" ]; then
            echo "✅ DEB package built successfully!"
            ls -lh "tscode-${DEB_VERSION}_${DEB_ARCH}.deb"
            sha256sum "tscode-${DEB_VERSION}_${DEB_ARCH}.deb" > "tscode-${DEB_VERSION}_${DEB_ARCH}.deb.sha256"
          else
            echo "❌ Failed to build DEB package"
            exit 1
          fi

      # ============================================================
      # 验证步骤
      # ============================================================
      - name: 🔍 Comprehensive ExtensionHost & System Verification
        if: always()
        run: |
          echo "=========================================="
          echo "🔍 COMPREHENSIVE EXTENSIONHOST VERIFICATION"
          echo "=========================================="

          BUILD_DIR="${BUILD_DIR:-./tscode}"
          PASSED=0
          FAILED=0

          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📌 1. ExtensionHost Core Files"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          if [ -f "$BUILD_DIR/resources/app/out/vs/workbench/api/node/extensionHostProcess.js" ]; then
              echo "   ✅ extensionHostProcess.js found"
              PASSED=$((PASSED + 1))
          else
              echo "   ❌ extensionHostProcess.js NOT FOUND"
              FAILED=$((FAILED + 1))
          fi

          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📌 2. Critical Extensions"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          EXT_DIR="$BUILD_DIR/resources/app/extensions"
          CRITICAL_EXTS=(
              "typescript-language-features"
              "git"
              "json-language-features"
              "markdown-language-features"
              "html-language-features"
              "css-language-features"
              "npm"
          )

          if [ -d "$EXT_DIR" ]; then
              EXT_COUNT=$(find "$EXT_DIR" -maxdepth 1 -type d ! -path "$EXT_DIR" 2>/dev/null | wc -l)
              echo "   📂 Extensions directory exists with $EXT_COUNT extensions"
              PASSED=$((PASSED + 1))

              for ext in "${CRITICAL_EXTS[@]}"; do
                  if [ -d "$EXT_DIR/$ext" ]; then
                      echo "   ✅ $ext"
                      PASSED=$((PASSED + 1))
                  else
                      echo "   ❌ $ext MISSING"
                      FAILED=$((FAILED + 1))
                  fi
              done
          else
              echo "   ❌ Extensions directory NOT found!"
              FAILED=$((FAILED + 1))
          fi

          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📌 3. Binary Permissions"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          if [ -f "$BUILD_DIR/chrome-sandbox" ]; then
              PERMS=$(stat -c %a "$BUILD_DIR/chrome-sandbox" 2>/dev/null || echo "000")
              if [ "$PERMS" = "4755" ]; then
                  echo "   ✅ chrome-sandbox SUID: 4755"
                  PASSED=$((PASSED + 1))
              else
                  echo "   ❌ chrome-sandbox permissions: $PERMS"
                  FAILED=$((FAILED + 1))
              fi
          fi

          if [ -f "$BUILD_DIR/tscode" ] && [ -x "$BUILD_DIR/tscode" ]; then
              echo "   ✅ tscode executable"
              PASSED=$((PASSED + 1))
          else
              echo "   ❌ tscode not executable"
              FAILED=$((FAILED + 1))
          fi

          echo ""
          echo "   🔍 Checking testagent and testflow:"
          TEST_BIN_FOUND=0
          for bin in testagent testflow; do
              BIN_PATH=$(find "$EXT_DIR" -name "$bin" -type f 2>/dev/null | head -1)
              if [ -n "$BIN_PATH" ]; then
                  if [ -x "$BIN_PATH" ]; then
                      echo "      ✅ $bin is executable"
                      TEST_BIN_FOUND=$((TEST_BIN_FOUND + 1))
                  else
                      echo "      ❌ $bin is NOT executable"
                      FAILED=$((FAILED + 1))
                  fi
              fi
          done

          if [ $TEST_BIN_FOUND -gt 0 ]; then
              echo "   ✅ Found $TEST_BIN_FOUND executable test binaries"
              PASSED=$((PASSED + 1))
          fi

          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📌 4. Keyring Bypass"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          if [ -f "$BUILD_DIR/run.sh" ]; then
              if grep -q "password-store=\"basic\"" "$BUILD_DIR/run.sh"; then
                  echo "   ✅ password-store=basic configured"
                  PASSED=$((PASSED + 1))
              fi
              if grep -q "no-sandbox" "$BUILD_DIR/run.sh"; then
                  echo "   ✅ --no-sandbox configured"
                  PASSED=$((PASSED + 1))
              fi
          fi

          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📌 5. node_modules"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          if [ -d "$BUILD_DIR/resources/app/node_modules" ]; then
              echo "   ✅ node_modules exists"
              PASSED=$((PASSED + 1))
          fi

          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 VERIFICATION SUMMARY"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "   ✅ Passed: $PASSED"
          echo "   ❌ Failed: $FAILED"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          if [ $FAILED -le 3 ]; then
              echo ""
              echo "✅✅✅ EXTENSIONHOST IS FUNCTIONAL!"
              echo ""
              echo "   🚀 To install:"
              echo "      sudo dpkg -i tscode-*.deb"
              echo "      sudo apt-get install -f"
              echo "      tscode"
              exit 0
          else
              echo ""
              echo "❌❌❌ ISSUES FOUND!"
              echo "   Failed checks: $FAILED"
              exit 1
          fi

      - name: 📤 Upload artifacts
        uses: actions/upload-artifact@v7
        with:
          name: tscode-debian10-deb
          path: |
            *.deb
            *.deb.sha256
          retention-days: 30

      - name: 📊 Build summary
        if: always()
        run: |
          echo "=========================================="
          echo "📊 BUILD SUMMARY"
          echo "=========================================="
          echo "Status: ${{ job.status }}"
          echo "Version: ${{ github.event.inputs.version || '1.116.0' }}"
          echo "Format: DEB (Debian 10)"
          echo ""
          echo "✅ Features:"
          echo "   - No password required on startup"
          echo "   - No sandbox password prompt"
          echo "   - Desktop entry in application menu"
          echo "   - Extension host fully functional"
          echo "   - Compatible native modules for Debian 10"
          echo "   - All extension binaries executable"
          echo "==========================================="
          if [ "${{ job.status }}" = "success" ]; then
            echo ""
            echo "✅ Build completed successfully!"
            ls -lh *.deb 2>/dev/null || echo "   No DEB package found"
            echo ""
            echo "📖 Installation:"
            echo "  sudo dpkg -i tscode-*.deb"
            echo "  sudo apt-get install -f"
            echo ""
            echo "🚀 After installation:"
            echo "  - Run: tscode (no password)"
            echo "  - Find in application menu: TSCode"
            echo "=========================================="
          fi