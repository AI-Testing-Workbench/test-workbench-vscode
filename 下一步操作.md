# 下一步操作

## 当前状态

已完成以下修改：

1. ✅ **better-sqlite3 错误** - 已修复并测试通过
2. ⏳ **file: 协议错误** - 已修改代码，使用 vscode-file:// 协议
3. ⏳ **Sentry IPC 错误** - 已禁用 Sentry，移除 sentry-ipc 协议

## 需要重新构建

由于修改了以下文件，需要重新构建：
- `extensions/aionui-main/src/renderer/main.tsx` (禁用 Sentry)
- `src/vs/aionui/electron-main/aionuiWindowManager.js` (协议处理 + 移除 Sentry)

## 执行步骤

### 方法 1：自动化脚本（推荐）

```bash
./rebuild-and-test.sh
```

这个脚本会：
1. 重新构建 AionUI
2. 重新构建 VS Code
3. 验证构建结果

### 方法 2：手动构建

```bash
# 1. 构建 AionUI
cd extensions/aionui-main
npm run package
cd ../..

# 2. 构建 VS Code
npm run gulp vscode-darwin-arm64
```

## 构建完成后测试

```bash
# 启动 VS Code
./test-vscode-aionui.sh
```

然后：
1. 按 `Cmd+Shift+P` 打开命令面板
2. 搜索 "AionUI"
3. 选择 "Open AionUI Window"
4. 检查 DevTools 中是否还有错误

## 预期结果

构建完成后，应该：
- ✅ 没有 sentry-ipc 错误
- ✅ 没有 file: 协议错误
- ✅ AionUI 窗口正常打开
- ✅ 可以看到 Gemini 代理

## 如果还有问题

运行日志检查脚本：
```bash
./.kiro/specs/aionui-integration/check-backend-logs.sh
```

查看详细的后端初始化日志和错误信息。
